import React from "react";
import ReactDOM from "react-dom";
import App from "./App";

// ReactDOM.render(App(), document.getElementById("root"));
//com babel posso executar a função como se fosse uma tag html, permitindo que ue passe propriedade diferentes, como se fossem atributos html
ReactDOM.render(<App />, document.getElementById("root"));

